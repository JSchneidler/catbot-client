import React from 'react';
import { Button } from '@material-ui/core';

const ReleaseControl = props =>
<Button {...props} color="secondary" variant="raised">
  Release Control
</Button>;

export default ReleaseControl;